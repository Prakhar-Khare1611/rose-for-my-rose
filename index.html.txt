<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Rose Day Surprise</title>
  <style>
    :root{
      --pink:#ffb6c1;
      --rose:#ff6f91;
      --peach:#ffd1dc;
      --lav:#e6d9ff;
      --bg1: linear-gradient(135deg, #ffd1dc, #ffe6f0, #fff1f6);
      --card: rgba(255,255,255,0.85);
      --shadow: 0 10px 25px rgba(0,0,0,.15);
      --glow: 0 0 20px rgba(255,105,180,.45);
    }
    *{box-sizing:border-box}
    html,body{margin:0;height:100%;font-family:system-ui,-apple-system,Segoe UI,Roboto}
    body{background:var(--bg1);overflow:hidden}

    .page{
      position:absolute; inset:0;
      display:none; align-items:center; justify-content:center;
      padding:16px;
      animation: fadeIn .6s ease forwards;
    }
    .page.active{display:flex}
    @keyframes fadeIn{from{opacity:0;transform:scale(.98)}to{opacity:1;transform:scale(1)}}

    /* Floating hearts & balloons */
    .bg-layer{position:absolute; inset:0; overflow:hidden; pointer-events:none}
    .float{
      position:absolute; bottom:-10%;
      font-size:20px; opacity:.8;
      animation: floatUp 10s linear infinite;
      filter: drop-shadow(0 6px 10px rgba(0,0,0,.2));
    }
    @keyframes floatUp{
      from{transform:translateY(0) translateX(0) scale(.9); opacity:.0}
      10%{opacity:.9}
      to{transform:translateY(-120vh) translateX(40px) scale(1.2); opacity:0}
    }

    .card{
      background:var(--card);
      border-radius:18px;
      padding:20px;
      box-shadow:var(--shadow);
      max-width:520px; width:100%;
      text-align:center;
      animation: breathe 3.5s ease-in-out infinite;
    }
    @keyframes breathe{0%,100%{transform:scale(1)}50%{transform:scale(1.02)}}

    .title{font-size:1.6rem;margin:8px 0 12px}
    .sub{opacity:.8;margin-bottom:10px}
    .btn{
      border:none; border-radius:999px; padding:12px 18px;
      background:linear-gradient(135deg,#ff7aa2,#ff4d79);
      color:#fff; font-weight:700; letter-spacing:.2px;
      box-shadow:var(--glow);
      animation:pulse 1.6s ease-in-out infinite;
    }
    @keyframes pulse{0%,100%{transform:scale(1)}50%{transform:scale(1.05)}}
    .btn:active{transform:scale(.98)}
    input{
      width:100%; padding:12px 14px; border-radius:12px; border:1px solid #f2b5c4;
      outline:none; font-size:1rem; margin:10px 0 14px;
    }

    /* Gifts */
    .gifts{display:grid; grid-template-columns:repeat(3,1fr); gap:12px}
    .gift{
      background:linear-gradient(135deg,#ffe0ea,#fff);
      border-radius:14px; padding:14px; cursor:pointer;
      box-shadow:var(--shadow);
      animation: bounce 2.2s ease-in-out infinite;
    }
    .gift:nth-child(2){animation-delay:.3s}
    .gift:nth-child(3){animation-delay:.6s}
    @keyframes bounce{0%,100%{transform:translateY(0)}50%{transform:translateY(-6px)}}
    .gift:hover{transform:scale(1.05)}

    /* Cat */
    .cat-wrap{
      position:relative; width:180px; height:180px; margin:0 auto 8px;
      animation: pop 0.7s cubic-bezier(.2,1.4,.3,1) forwards, breathe 3s ease-in-out infinite .8s;
    }
    @keyframes pop{from{transform:translateY(60px) scale(.7); opacity:0}to{transform:translateY(0) scale(1); opacity:1}}
    .cat{
      width:100%; height:100%; border-radius:50%;
      background:radial-gradient(circle at 30% 30%, #fff, #ffd6e3);
      display:flex; align-items:center; justify-content:center; font-size:72px;
      box-shadow:var(--shadow);
    }
    .rose{font-size:28px; margin-top:-10px; filter: drop-shadow(0 0 10px rgba(255,0,0,.4))}

    /* Envelope */
    .envelope{
      width:220px; height:140px; margin:0 auto 10px; position:relative; cursor:pointer;
      animation: breathe 2.6s ease-in-out infinite;
    }
    .env-body{
      position:absolute; inset:0; background:#fff; border-radius:12px; box-shadow:var(--shadow);
    }
    .env-flap{
      position:absolute; top:0; left:0; right:0; height:70px;
      background:linear-gradient(135deg,#ffd1dc,#fff);
      clip-path: polygon(0 0, 100% 0, 50% 70%);
      transform-origin:top; transition:transform .6s ease;
    }
    .stamp{
      position:absolute; right:10px; top:10px; background:#ff7aa2; color:#fff;
      padding:6px 8px; border-radius:6px; font-size:12px;
      box-shadow:var(--glow);
    }
    .letter{
      position:absolute; inset:10px; background:#fff; border-radius:10px;
      padding:12px; transform:translateY(30px); opacity:0; transition:.6s ease;
      overflow:auto;
    }
    .open .env-flap{transform:rotateX(160deg)}
    .open .letter{transform:translateY(-10px); opacity:1}

    /* Utilities */
    .spacer{height:10px}
    .hidden{display:none}
  </style>
</head>
<body>

  <!-- Floating background -->
  <div class="bg-layer" id="bg"></div>

  <!-- Page 1 -->
  <section class="page active" id="p1">
    <div class="card">
      <div class="title">Enter Your Name 💖</div>
      <div class="sub">A tiny surprise awaits</div>
      <input id="nameInput" placeholder="Her name..." />
      <button class="btn" onclick="go(2)">Continue 💝</button>
    </div>
  </section>

  <!-- Page 2 -->
  <section class="page" id="p2">
    <div class="card">
      <div class="title">Choose a Gift for You 💘</div>
      <div class="spacer"></div>
      <div class="gifts">
        <div class="gift" onclick="pick()">🎁</div>
        <div class="gift" onclick="pick()">🎁</div>
        <div class="gift" onclick="pick()">🎁</div>
      </div>
    </div>
  </section>

  <!-- Page 3 -->
  <section class="page" id="p3">
    <div class="card">
      <div class="cat-wrap">
        <div class="cat">🐱</div>
      </div>
      <div class="rose">🌹</div>
      <div class="title" id="romance"></div>
      <div class="spacer"></div>
      <button class="btn" onclick="go(4)">Open Your Letter 💌</button>
    </div>
  </section>

  <!-- Page 4 -->
  <section class="page" id="p4">
    <div class="card">
      <div class="envelope" id="env" onclick="openLetter()">
        <div class="env-body"></div>
        <div class="env-flap"></div>
        <div class="stamp">LOVE</div>
        <div class="letter" id="letter">
          <strong>My love <span id="lname"></span>,</strong><br><br>
          You are the most beautiful part of my life.  
          Every smile of yours makes my heart softer.  
          Thank you for being my peace and my favorite person.  
          <br><br>
          Happy Rose Day 🌹  
          <br><br>
          — Yours, forever 💖
        </div>
      </div>
      <div class="sub">Tap the envelope 💕</div>
    </div>
  </section>

  <script>
    const bg = document.getElementById('bg');
    const hearts = ["💖","💗","💘","💝","🎈","💞","✨"];
    function spawn(){
      const s = document.createElement('div');
      s.className = 'float';
      s.textContent = hearts[Math.floor(Math.random()*hearts.length)];
      s.style.left = Math.random()*100 + 'vw';
      s.style.animationDuration = (8 + Math.random()*6) + 's';
      s.style.fontSize = (16 + Math.random()*24) + 'px';
      bg.appendChild(s);
      setTimeout(()=>s.remove(), 14000);
    }
    setInterval(spawn, 600);

    let herName = "";
    function go(n){
      if(n===2){
        herName = document.getElementById('nameInput').value || "My Love";
      }
      document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
      document.getElementById('p'+n).classList.add('active');
      if(n===3){
        document.getElementById('romance').innerHTML =
          `This rose is for you, <b>${herName}</b> 💖<br/>You make my world brighter every day.`;
      }
      if(n===4){
        document.getElementById('lname').textContent = herName;
      }
    }
    function pick(){ go(3); }
    function openLetter(){
      document.getElementById('env').classList.toggle('open');
    }
  </script>
</body>
</html>
